# Database Connection Configuration
# To use your PostgreSQL database, uncomment the line below and update the credentials:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/assetflow

# Default SQLite database fallback for quick local testing and development
DATABASE_URL=sqlite:///./assetflow.db
